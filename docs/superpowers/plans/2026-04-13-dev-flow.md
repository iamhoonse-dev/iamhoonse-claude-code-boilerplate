# 개발 플로우 셋업 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Superpowers 기반 5단계 개발 워크플로우와 GitHub 거버넌스를 보일러플레이트에 셋업한다.

**Architecture:** 기존 CLAUDE.md와 CONTRIBUTING.md를 강화하고, .github/ 디렉토리에 PR/이슈 템플릿을 추가하며, 권한 설정 예시 파일을 제공한다. 모든 파일은 Markdown 또는 YAML/JSON 형식이다.

**Tech Stack:** Markdown, YAML, JSON

---

## 파일 구조

- Modify: `CLAUDE.md` — 개발 워크플로우, 브랜치 전략, 커밋 컨벤션 추가
- Modify: `CONTRIBUTING.md` — 개발 워크플로우 섹션 추가, PR 가이드라인 수정
- Create: `.github/PULL_REQUEST_TEMPLATE.md` — PR 템플릿
- Create: `.github/ISSUE_TEMPLATE/bug-report.yml` — 버그 리포트 템플릿
- Create: `.github/ISSUE_TEMPLATE/feature-request.yml` — 기능 요청 템플릿
- Create: `.github/ISSUE_TEMPLATE/documentation-improvement.yml` — 문서 개선 템플릿
- Create: `.claude/settings.local.json.example` — 권한 설정 예시

---

### Task 1: CLAUDE.md 강화

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: CLAUDE.md를 5단계 워크플로우 포함 내용으로 교체**

전체 내용을 다음으로 교체한다:

```markdown
# Project Guide

이 프로젝트는 iamhoonse-claude-code 마켓플레이스와 공식 플러그인으로 사전 설정되어 있습니다.

## 개발 워크플로우

모든 기능 개발 및 버그 수정은 다음 5단계를 따릅니다:

1. **Brainstorming** (`superpowers:brainstorming`)
   - 요구사항 파악, 설계 옵션 탐색, 설계 문서 작성

2. **Planning** (`superpowers:writing-plans`)
   - 설계 기반 구현 계획 수립, 태스크 분해

3. **Implementation** (`superpowers:test-driven-development`, `superpowers:executing-plans`)
   - TDD 기반 구현 (RED → GREEN → REFACTOR)

4. **Code Review** (`superpowers:requesting-code-review`)
   - 구현 결과 검증, 품질 확인

5. **Branch Completion** (`superpowers:finishing-a-development-branch`)
   - 머지, PR 생성, 또는 정리

## 브랜치 전략

> `git-convention` 플러그인이 브랜치 네이밍을 자동으로 검증합니다.

- `main` 브랜치는 보호됩니다. 직접 push하지 마세요.
- 작업 브랜치는 다음 네이밍을 따릅니다:
  - `feat/<name>` — 새 기능 추가
  - `fix/<name>` — 버그 수정
  - `docs/<name>` — 문서 변경
  - `chore/<name>` — 기타 유지보수

## 커밋 컨벤션

> `git-convention` 플러그인이 커밋 메시지를 자동으로 검증하고,
> `commit-commands` 플러그인이 커밋 명령어를 지원합니다.

[Conventional Commits](https://www.conventionalcommits.org/) 형식을 사용합니다:

```
<type>: <description>
```

타입: `feat`, `fix`, `docs`, `chore`, `refactor`

## 설정 구조

- `.claude/settings.json` — 공유 설정 (마켓플레이스 등록, 플러그인 활성화)
- `.claude/settings.local.json` — 개인 설정 (git에서 제외됨)
- `.claude/settings.local.json.example` — 개인 설정 예시 (권한 설정 참고용)
```

- [ ] **Step 2: 커밋**

```bash
git add CLAUDE.md
git commit -m "docs: enhance CLAUDE.md with dev workflow and conventions"
```

---

### Task 2: CONTRIBUTING.md 업데이트

**Files:**
- Modify: `CONTRIBUTING.md`

- [ ] **Step 1: CONTRIBUTING.md를 워크플로우 포함 내용으로 교체**

전체 내용을 다음으로 교체한다:

```markdown
# 기여 가이드

이 프로젝트에 기여해 주셔서 감사합니다! 아래 가이드라인을 참고해 주세요.

## 시작하기

1. 이 저장소를 **Fork** 합니다.
2. Fork한 저장소를 clone합니다:
   ```bash
   git clone https://github.com/<your-username>/iamhoonse-claude-code-boilerplate.git
   cd iamhoonse-claude-code-boilerplate
   ```
3. 작업용 브랜치를 생성합니다:
   ```bash
   git checkout -b feat/your-feature-name
   ```

## 개발 워크플로우

이 프로젝트는 Superpowers 플러그인 기반의 5단계 개발 워크플로우를 따릅니다:

| 단계 | Superpowers 스킬 | 설명 |
|------|------------------|------|
| 1. Brainstorming | `superpowers:brainstorming` | 요구사항 파악, 설계 옵션 탐색, 설계 문서 작성 |
| 2. Planning | `superpowers:writing-plans` | 설계 기반 구현 계획 수립, 태스크 분해 |
| 3. Implementation | `superpowers:test-driven-development` | TDD 기반 구현 (RED → GREEN → REFACTOR) |
| 4. Code Review | `superpowers:requesting-code-review` | 구현 결과 검증, 품질 확인 |
| 5. Branch Completion | `superpowers:finishing-a-development-branch` | 머지, PR 생성, 또는 정리 |

## 플러그인 관리

### 플러그인 추가

`.claude/settings.json`의 `enabledPlugins`에 항목을 추가합니다:

```json
{
  "enabledPlugins": {
    "plugin-name@marketplace-name": true
  }
}
```

플러그인 이름은 `플러그인명@마켓플레이스명` 형식을 따릅니다:
- 공식 마켓플레이스: `plugin-name@claude-plugins-official`
- 커스텀 마켓플레이스: `plugin-name@iamhoonse-claude-code`

### 플러그인 제거

`enabledPlugins`에서 해당 항목을 삭제하거나 값을 `false`로 변경합니다.

### 호환성 주의사항

새 플러그인을 추가할 때 기존 플러그인과의 충돌 여부를 확인해 주세요.
특히 Superpowers 플러그인과 워크플로우가 겹치는 플러그인(예: 자체 코드 리뷰, 디버깅, 계획 워크플로우를 제공하는 플러그인)은 충돌할 수 있습니다.

## 브랜치 전략

- `main` 브랜치는 보호됩니다. 직접 push하지 마세요.
- 작업 브랜치는 다음 네이밍을 따릅니다:
  - `feat/<기능명>` — 새 기능 추가
  - `fix/<수정내용>` — 버그 수정
  - `docs/<문서명>` — 문서 변경
  - `chore/<작업명>` — 기타 유지보수

## 커밋 컨벤션

[Conventional Commits](https://www.conventionalcommits.org/) 형식을 사용합니다:

```
<type>: <description>
```

### 타입

| 타입 | 용도 |
|-----|------|
| `feat` | 새 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 변경 |
| `chore` | 빌드, 설정 등 기타 변경 |
| `refactor` | 리팩토링 (기능 변경 없음) |

### 예시

```
feat: enable playwright plugin
docs: update README plugin list
fix: correct marketplace source URL
chore: update gitignore patterns
```

## PR 가이드라인

### PR 제목

Conventional Commits 형식으로 작성합니다:

```
feat: 새 기능 설명
fix: 수정 내용 설명
docs: 문서 변경 설명
```

### PR 작성 시 포함할 내용

- **변경 사항 요약**: 무엇을 왜 변경했는지
- **기능 체크리스트**: 구현한 기능이나 수정 사항 나열
- **워크플로우 체크리스트**: 5단계 워크플로우 준수 여부
- **참고 문서**: 관련 설계 문서, 구현 계획, 이슈 링크

### 리뷰 프로세스

1. PR을 생성하면 리뷰어가 배정됩니다.
2. 리뷰 피드백이 있으면 반영 후 재요청합니다.
3. 승인 후 `main` 브랜치에 머지됩니다.
```

- [ ] **Step 2: 커밋**

```bash
git add CONTRIBUTING.md
git commit -m "docs: update CONTRIBUTING with dev workflow and PR guidelines"
```

---

### Task 3: PR 템플릿 생성

**Files:**
- Create: `.github/PULL_REQUEST_TEMPLATE.md`

- [ ] **Step 1: .github 디렉토리 생성 확인**

```bash
mkdir -p .github
```

- [ ] **Step 2: PR 템플릿 파일 작성**

`.github/PULL_REQUEST_TEMPLATE.md` 파일을 다음 내용으로 생성한다:

```markdown
## 요약

<!-- 변경 사항을 간단히 설명해 주세요 -->
<!-- PR 제목은 Conventional Commits 형식으로 작성합니다: feat: 작업 내용 -->

Closes #

## 기능 체크리스트

- [ ] <!-- 구현한 기능이나 수정 사항을 나열해 주세요 -->

## 워크플로우 체크리스트

- [ ] Brainstorming (`superpowers:brainstorming`) 완료
- [ ] Planning (`superpowers:writing-plans`) 완료
- [ ] Implementation — TDD 기반 구현 완료
- [ ] Code Review (`superpowers:requesting-code-review`) 완료
- [ ] Branch Completion (`superpowers:finishing-a-development-branch`) 완료

## 참고 문서

<!-- 관련 설계 문서, 구현 계획, 이슈 등을 링크해 주세요 -->

-
```

- [ ] **Step 3: 커밋**

```bash
git add .github/PULL_REQUEST_TEMPLATE.md
git commit -m "chore: add PR template with workflow checklist"
```

---

### Task 4: 이슈 템플릿 생성

**Files:**
- Create: `.github/ISSUE_TEMPLATE/bug-report.yml`
- Create: `.github/ISSUE_TEMPLATE/feature-request.yml`
- Create: `.github/ISSUE_TEMPLATE/documentation-improvement.yml`

- [ ] **Step 1: ISSUE_TEMPLATE 디렉토리 생성**

```bash
mkdir -p .github/ISSUE_TEMPLATE
```

- [ ] **Step 2: bug-report.yml 작성**

`.github/ISSUE_TEMPLATE/bug-report.yml` 파일을 다음 내용으로 생성한다:

```yaml
name: "버그 리포트"
description: 버그를 보고합니다.
labels: ["bug"]
body:
  - type: textarea
    id: description
    attributes:
      label: 버그 설명
      description: 어떤 버그인지 간략히 설명해주세요.
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: 재현 단계
      description: 버그를 재현할 수 있는 단계를 작성해주세요.
      value: |
        1.
        2.
        3.
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: 기대 동작
      description: 정상적으로 동작했을 때 어떤 결과를 기대하는지 설명해주세요.
    validations:
      required: true

  - type: textarea
    id: actual
    attributes:
      label: 실제 동작
      description: 실제로 어떤 결과가 나타나는지 설명해주세요.
    validations:
      required: true

  - type: textarea
    id: environment
    attributes:
      label: 환경 정보
      description: OS, 관련 도구 버전 등 환경 정보를 작성해주세요.
    validations:
      required: false
```

- [ ] **Step 3: feature-request.yml 작성**

`.github/ISSUE_TEMPLATE/feature-request.yml` 파일을 다음 내용으로 생성한다:

```yaml
name: "기능 요청"
description: 새로운 기능을 요청합니다.
labels: ["enhancement"]
body:
  - type: textarea
    id: problem
    attributes:
      label: 문제 상황
      description: 어떤 문제나 불편함을 해결하고 싶은지 설명해주세요.
    validations:
      required: true

  - type: textarea
    id: solution
    attributes:
      label: 제안하는 해결책
      description: 어떤 방식으로 해결하면 좋을지 제안해주세요.
    validations:
      required: true

  - type: textarea
    id: alternatives
    attributes:
      label: 대안 검토
      description: 다른 방법을 검토했다면 설명해주세요.
    validations:
      required: false
```

- [ ] **Step 4: documentation-improvement.yml 작성**

`.github/ISSUE_TEMPLATE/documentation-improvement.yml` 파일을 다음 내용으로 생성한다:

```yaml
name: "문서 개선"
description: 문서의 개선 사항을 제안합니다.
labels: ["documentation"]
body:
  - type: input
    id: target-doc
    attributes:
      label: 대상 문서
      description: 개선이 필요한 문서를 지정해주세요. (예: README.md, CONTRIBUTING.md)
      placeholder: README.md
    validations:
      required: true

  - type: textarea
    id: problem
    attributes:
      label: 현재 문제점
      description: 현재 문서에서 부족하거나 잘못된 부분을 설명해주세요.
    validations:
      required: true

  - type: textarea
    id: suggestion
    attributes:
      label: 개선 제안
      description: 어떻게 개선하면 좋을지 제안해주세요.
    validations:
      required: true
```

- [ ] **Step 5: 커밋**

```bash
git add .github/ISSUE_TEMPLATE/bug-report.yml .github/ISSUE_TEMPLATE/feature-request.yml .github/ISSUE_TEMPLATE/documentation-improvement.yml
git commit -m "chore: add issue templates for bug, feature, and docs"
```

---

### Task 5: settings.local.json.example 생성

**Files:**
- Create: `.claude/settings.local.json.example`

- [ ] **Step 1: 예시 파일 작성**

`.claude/settings.local.json.example` 파일을 다음 내용으로 생성한다:

```json
{
  "permissions": {
    "allow": [
      "WebFetch(domain:raw.githubusercontent.com)",
      "WebFetch(domain:api.github.com)",
      "WebFetch(domain:docs.anthropic.com)",
      "Bash(git add:*)",
      "Bash(git commit -m ':*)",
      "Bash(git push:*)"
    ]
  }
}
```

- [ ] **Step 2: JSON 유효성 검증**

Run: `python3 -m json.tool .claude/settings.local.json.example > /dev/null && echo "Valid JSON"`
Expected: `Valid JSON`

- [ ] **Step 3: 커밋**

```bash
git add .claude/settings.local.json.example
git commit -m "chore: add settings.local.json.example with permission presets"
```
